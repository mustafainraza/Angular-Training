/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CustomerRowHoverDirective } from './customerRowHover.directive';

describe('Directive: CustomerRowHover', () => {
  it('should create an instance', () => {
    const directive = new CustomerRowHoverDirective();
    expect(directive).toBeTruthy();
  });
});
